id: no-panic
language: go
severity: error
message: Do not use panic() in service code — return an error instead.
note: panic is only appropriate in init() or truly unrecoverable situations.
rule:
  kind: call_expression
  has:
    kind: identifier
    regex: ^panic$
